# k8s/code-review.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: code-review-db-secret
  namespace: skillbridge
stringData:
  POSTGRES_PASSWORD: "postgres"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: code-review-config
  namespace: skillbridge
data:
  PORT: "4105"
  DATABASE_URL: "postgresql://postgres:postgres@code-review-db:5432/code_review_db?schema=public"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: code-review-db
  namespace: skillbridge
spec:
  serviceName: "code-review-db"
  replicas: 1
  selector:
    matchLabels:
      app: code-review-db
  template:
    metadata:
      labels:
        app: code-review-db
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: code-review-db-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "code_review_db"
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: code-review-db
  namespace: skillbridge
spec:
  selector:
    app: code-review-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-review-deployment
  namespace: skillbridge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: code-review
  template:
    metadata:
      labels:
        app: code-review
    spec:
      containers:
      - name: code-review
        image: us-central1-docker.pkg.dev/skillbridge-demo/skillbridge-repo/code-review-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4105
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: code-review-config
              key: PORT
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: code-review-config
              key: DATABASE_URL
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        livenessProbe:
          httpGet:
            path: /health
            port: 4105
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4105
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: code-review-service
  namespace: skillbridge
spec:
  selector:
    app: code-review
  ports:
    - protocol: TCP
      port: 4105
      targetPort: 4105
  type: ClusterIP
---
apiVersion: batch/v1
kind: Job
metadata:
  name: code-review-migrate
  namespace: skillbridge
spec:
  template:
    spec:
      containers:
      - name: migrate
        image: us-central1-docker.pkg.dev/skillbridge-demo/skillbridge-repo/code-review-service:latest
        command: ["sh", "-c", "npx prisma migrate deploy"]
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: code-review-config
              key: DATABASE_URL
      restartPolicy: OnFailure
  backoffLimit: 1
