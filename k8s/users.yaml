# k8s/users.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: users-db-secret
  namespace: skillbridge
stringData:
  POSTGRES_PASSWORD: "postgres"   # replace with strong password in production
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: users-config
  namespace: skillbridge
data:
  PORT: "4101"
  DATABASE_URL: "postgresql://postgres:postgres@users-db:5432/users_db?schema=public"
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: users-db
  namespace: skillbridge
spec:
  serviceName: "users-db"
  replicas: 1
  selector:
    matchLabels:
      app: users-db
  template:
    metadata:
      labels:
        app: users-db
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: users-db-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "users_db"
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: users-db
  namespace: skillbridge
spec:
  selector:
    app: users-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
  namespace: skillbridge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
      - name: users
        image: us-central1-docker.pkg.dev/skillbridge-demo/skillbridge-repo/users-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4101
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: users-config
              key: PORT
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: users-config
              key: DATABASE_URL
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
        livenessProbe:
          httpGet:
            path: /health
            port: 4101
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4101
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: skillbridge
spec:
  selector:
    app: users
  ports:
    - protocol: TCP
      port: 4101
      targetPort: 4101
  type: ClusterIP
---
apiVersion: batch/v1
kind: Job
metadata:
  name: users-migrate
  namespace: skillbridge
spec:
  template:
    spec:
      containers:
      - name: migrate
        image: us-central1-docker.pkg.dev/skillbridge-demo/skillbridge-repo/users-service:latest
        command: ["sh", "-c", "npx prisma migrate deploy"]
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: users-config
              key: DATABASE_URL
      restartPolicy: OnFailure
  backoffLimit: 1
